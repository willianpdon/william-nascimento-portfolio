{
  "name": "portfolio-infra",
  "private": true,
  "version": "0.1.0",
  "description": "AWS CDK app that provisions the static hosting infrastructure (S3 + CloudFront + GitHub OIDC deploy role) for the William Nascimento portfolio.",
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "bootstrap": "cdk bootstrap"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "aws-cdk": "^2.166.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.166.0",
    "constructs": "^10.4.2"
  }
}
